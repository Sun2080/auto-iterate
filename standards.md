---
name: auto-iterate-standards
description: 给宿主项目 Claude Code agent 的最小写作 + 锚定守则。每次动手前读一遍。
audience: host-project agent
---

# Standards

三部分：
- **A** — Karpathy 4 原则（每次编辑都要过的闸门）
- **B** — Opus 4.7 交互契约（字面理解指令 → prompt 和自我陈述都要符合）
- **C** — 唯一迭代硬线（防跑偏）

---

## A · Karpathy 4 原则

### A1. Think Before Coding

不假设、不藏糊涂、暴露取舍。

- 明说假设，不确定就问
- 多种解读并存 → 列出来，别偷偷挑一个
- 有更简方案就指出来，该顶回去就顶
- 不清楚就停，说清**什么地方糊涂**再问

反模式：看到模糊需求直接开写；用「我觉得用户的意思是」填空。

### A2. Simplicity First

解决问题的最少代码，不投机。

- 没要求的特性不加
- 只用一次的代码不抽象
- 没要求的「灵活性 / 可配置」不加
- 不会发生的分支不写错误处理
- 200 行能用 50 行写完 → 重写

自问：「资深工程师会说这过度设计吗？」会 → 简化。

### A3. Surgical Changes

只碰必须碰的，只收拾自己弄出的烂摊子。

- 不「顺手」改相邻代码、注释、格式
- 不重构没坏的东西
- 跟既有风格，哪怕你觉得丑
- 看到无关死代码就**提一句，不删**
- 清理**你制造**的未用 import / 变量 / 函数

铁律：每一行改动都能直接追溯到用户请求。追溯不到就不该改。

### A4. Goal-Driven Execution

先定成功判据，循环直到验证通过。

动工前陈述一个短计划：目标（一句话）· 步骤（≤ 5 条）· **验证方式**（跑什么命令 / 看什么输出 / 什么结果算成功）。

没有验证方式的任务 = 不可完成的任务，补齐再开工。完成时自检：验证真跑过了吗？输出真的符合判据吗？「应该能跑」= 没跑 = 未完成。

**UI / 前端改动**：用 Playwright MCP 真跑。编译过 + 类型过 ≠ UI 可用。4.7 有时会吐能编译但交互坏的代码 —— 必须跑。

---

## B · Opus 4.7 交互契约

### B1. 字面理解指令

4.6 会替用户补全隐含意图，4.7 不会。所以：

- **前置给全上下文**：intent / 约束 / 成功判据 / 关键文件位置，第一轮就写清楚，不要分多轮渐进披露
- **不模糊**：「优化一下」「改好看点」不是指令，是邀请乱搞。指令要带判据：「函数 X 从 O(n²) 降到 O(n log n)，tests/perf_test.py 通过」
- **明说要不要激进工具用**：4.7 默认少调工具用自己推理。需要大量 grep/read 时 prompt 里写「主动搜索代码库，不要基于假设」
- **库 / API 问题用 Context7 MCP**：4.7 知识有 cutoff。涉及第三方库版本、新 API、CLI 语法时**优先** `mcp__context7__resolve-library-id` → `query-docs`，不要凭记忆推测

### B2. 自适应思考

- **不要设固定 thinking budget**。4.7 自己决定何时多想，预设反而干扰
- **给任务总 budget 提示**：「这一轮预算 ~20k tokens」—— 4.7 看得见倒计时，会自己配速
- **长任务当成「委派给一个能干的工程师」**，不是对话式结对编程。一次性给全指令，让它跑，别中间插话

### B3. 自发验证

- 验证脚本要能跑 —— 没有 `npm test` / `pytest` 就第一轮先补
- 验证失败 → 自己 fix → 再验证 → 通过才 surface 结果，不要把红色 CI 交给用户
- **绝不**把「编译通过」当成「功能正确」

---

## C · 唯一迭代硬线 —— GOALS 锚定

动手前自问一句：

> 这一步能在 `GOALS.md` 的某条下找到归属吗？

**找不到 → 停下问人**。两种可能：

- **目标过时** → 问用户要不要更新 `GOALS.md`
- **我在自娱自乐** → 回退到上一个对齐目标的 commit，重新规划

**绝不**自己扩张目标。「顺手优化」「发现了更有趣的问题」都是跑偏信号。

`GOALS.md` 由**人**写。Agent 不自创方向。没有 `GOALS.md` 就不开始工作 —— 先引导用户按 [`.claude/templates/GOALS.md.template`](.claude/templates/GOALS.md.template) 填。

---

## 自检清单（commit 前过一遍）

- [ ] 每一行改动都能追溯到任务 / GOALS？
- [ ] 有没有加不需要的抽象 / 错误处理 / 特性？
- [ ] 验证真的跑过？输出符合判据？
- [ ] 风格跟既有代码一致？
- [ ] 只处理了自己制造的孤儿，没动无关死代码？
