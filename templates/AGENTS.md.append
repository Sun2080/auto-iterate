<!--
  把下面这段追加到宿主项目根的 AGENTS.md 文件末尾。
  如果没有 AGENTS.md，就新建一个，只包含这段。
  如果项目已用其他指令文件名，可按 Codex 配置 fallback filenames；默认推荐 AGENTS.md。
-->

## 代码与迭代标准（by auto-iterate）

本项目遵循 `auto-iterate` 规范：

- **动手前**读 `.claude/standards/code.md` —— Karpathy 4 原则 + Agent 交互契约
- **开始迭代循环前**读 `.claude/standards/iterate.md` —— 核心循环、复利机制、停止条件
- **本项目已有自己的迭代约定**（NORTH_STAR / HANDOFF / DECISIONS 等）？读 `.claude/standards/iterate.md §11`，按角色映射表整合，不要两套并维护

### 本项目的迭代记忆

- `GOALS.md` —— 目标与成功判据（由人维护）
- `AGENT_MEMORY.md` —— 累积的模式 / 坑 / 决策（agent 维护，≤ 200 行）
- `progress.md` —— 每轮日志（agent 追加）

### 硬约束提醒（从 iterate.md 摘）

- 每轮至少一次 git commit（失败实验也要 commit 再 revert）
- 先 commit 再 verify
- 连续 2 轮同错 → 停下问人
- 每 6 轮做 mini-review + prune AGENT_MEMORY
- 每 30 轮大回：产出战报 · 停下等人类
- 没有用户明确授权，不要自己创建 automation 或计划任务

### 不做的事（从本项目 Non-Goals）

见 `GOALS.md` 的 `Non-Goals`。Agent 若动了这些，视为跑偏（iterate.md §7），立即回退到仍对齐目标的状态。
