<!--
  auto-iterate installed · sha <REPLACE_ME_HEAD_SHA> · <REPLACE_ME_YYYY-MM-DD>
  装时 agent 会把上行的占位替换成实际 clone 到的 HEAD sha + 当天日期。
  下次重装时 agent 靠这个注释定位本段落整体替换,不追加第二份。
-->

## AI 基座 · by auto-iterate

本项目使用 [auto-iterate](https://github.com/Sun2080/auto-iterate) 作为 AI 基座。两种工作模式:

### 默认 · 有人值守

- 动手前读 [.claude/standards.md](.claude/standards.md) —— Karpathy 4 原则 + Opus 4.7 契约 + GOALS 锚定硬线
- 方向由 [GOALS.md](GOALS.md) 提供(由人维护)
- 每次动手前自问「这一步能在 GOALS.md 的某条下找到归属吗?」找不到 → 停下问人,**不自己扩张方向**

### Autopilot · 无人参与

用户消息含 `自动迭代` / `自动跑` / `自主跑` / `托管` / `autopilot` 触发词时,**加载** [.claude/autopilot.md](.claude/autopilot.md),按其协议执行。

**启动口令速查**:

```
自动迭代 4h                           # Mission 从 GOALS.md 读
自动迭代 50 commit
自动迭代 到 2026-04-22 03:00
自动迭代 到明晚 22:00
自动迭代:<mission>,限 <上限>           # 首启 / inline Mission
```

**硬线**:
- Mission 是唯一硬线(由人给);Success Criteria / Non-Goals 允许 bootstrap 自填
- 上限必给(时间 / 绝对时间 / commit / 预算任一),否则不启动
- 硬停时写 HANDOFF 到 `progress.md` 尾,**不**续 cron / ScheduleWakeup
