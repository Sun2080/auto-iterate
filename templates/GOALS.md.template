# Goals — <宿主项目名>

> **有人值守模式**:Mission / Success Criteria / Non-Goals / Constraints 全部**由人写**,agent 不自创方向。
> **Autopilot 模式**(`自动迭代` / `托管` 等触发词启动):**Mission 由用户给**(填本文件,或首启时 inline `自动迭代:<mission>,限 <上限>`);Success Criteria / Non-Goals / Constraints 允许 agent 首轮 bootstrap 自填。
> 没有 Mission 就不开始工作。

## Mission

<一句话。这个项目要做什么?解决谁的什么问题?>

**Mission 是硬线**:agent 不可动,autopilot 模式下偏 Mission 就硬停。

示例:
- 「为 QMT 量化平台提供自动化迭代的监控与告警子系统。」
- 「做黄金交易辅助系统,给散户看价 / 看趋势 / 算仓位。」

## Success Criteria

**Agent 用这里的打勾情况判断「达成,停下」**。

- [ ] <成功判据 1:可验证、可打勾、有机械检查方法>
- [ ] <成功判据 2>
- [ ] <成功判据 3>

判据写法要带「怎么验证」:
- ❌ 「性能更好」— 没判据
- ✔ 「`tests/perf.py` 通过,P99 < 200ms」— 有判据

## Non-Goals

明说**不要**做的事,agent 才不会自己扩展。

- 不做 <X>
- 不重构 <Y>
- 不引入 <Z>

## Constraints

- **技术约束**: <语言/框架版本、依赖限制>
- **风格约束**: <代码风格、目录结构要求>
- **流程约束**: <分支策略、PR 要求、哪些动作必须人类审批>

## Context / Background

<可选。简述背景让 agent 理解为什么这个项目存在、上下文是什么。>

## Discoveries

<!-- Autopilot 模式下 agent 追加。每条格式:
### <短标题>
**做了啥**: <一句话>
**结果**: <数据 / 验证结果 / commit sha>
**启发**: <下一步 / 复用点 / 避坑>
-->

---

## 首轮指引(可选)

如果希望 agent 首轮优先做 X,在这里明说:

> 首轮请先 <动作>,产出 <文件>,然后进入正常循环。
