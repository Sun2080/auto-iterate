# Goals — <宿主项目名>

> **这份文件由人类写**。Agent 不自创方向。写完删掉本引用块与占位符。
> 没写完就别开 `iterate.md` 的循环。

## Mission

<一句话。这个项目要做什么？解决谁的什么问题？>

示例：
- 「为 QMT 量化平台提供自动化迭代的监控与告警子系统。」
- 「把博客的 Markdown 源文件重构为可索引的个人知识库。」

## Success Criteria

**Agent 用这里的打勾情况判断「任务达成，停下」**（见 `iterate.md §6`）。

- [ ] <成功判据 1：可验证、可打勾、有机械检查方法>
- [ ] <成功判据 2>
- [ ] <成功判据 3>

判据写法要带「怎么验证」：
- ❌ 「性能更好」— 没判据
- ✔ 「`tests/perf.py` 通过，P99 < 200ms」— 有判据

## Non-Goals

明说**不要**做的事，agent 才不会自己扩展。

- 不做 <X>
- 不重构 <Y>
- 不引入 <Z>

## Constraints

- **技术约束**: <语言/框架版本、依赖限制>
- **风格约束**: <代码风格、目录结构要求>
- **流程约束**: <分支策略、PR 要求、哪些动作必须人类审批>

## Context / Background

<可选。简述背景让 agent 理解为什么这个项目存在、上下文是什么。>

---

## 首轮指引（可选）

如果希望 agent 首轮优先做 X，在这里明说：

> 首轮请先 <动作>，产出 <文件>，然后进入正常循环。
